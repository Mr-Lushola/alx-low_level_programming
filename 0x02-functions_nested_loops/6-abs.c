#include "main.h"
/**
 * _abs - function to compute the absolute value of int
 * @n: integer of the program
 *
 * Return: absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
