#include <stdio.h>
/**
 * main - entry point
 * return : always return 0 (success)
 * (0) - return value for main
 */
int main(void)
{
	puts("\"Programming is like building a multillingual puzzle");
	return (0);
}
